function Mostrar()
{
	var edad;
	edad=document.getElementById("edad").value;
	if (edad==15)
	{
		alert("niña bonita");
	}
}

//FIN DE LA FUNCIÓN
/* al usar if es necesario primero anunciar la variable en la que se va a trabajar, en este caso la edad, 
luego se le da su valor, una vez que ponemos el if se abre una nueva llave y se cierra luego.*/
